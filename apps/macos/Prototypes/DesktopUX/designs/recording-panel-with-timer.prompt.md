# Recording panel: selected direction with elapsed time

Generated with the built-in ImageGen tool on 2026-09-08.

The owner selected the first displayed compact-panel concept, requested a small monospaced elapsed-time display above the level indicator, and approved this revised image as the base. This image is a design reference, not capture evidence. The disposable native prototype follows this layout; its verification status is documented separately.

Input: `recording-panel-selected-base.png`

Output: `recording-panel-with-timer.png`

## Exact edit prompt

Use case: precise-object-edit / UI mockup revision.
Edit the attached selected Trigo floating recording panel with ONE small addition only.

Add the elapsed recording time "04:12", centered horizontally directly ABOVE the cyan segmented application-audio level meter, inside the existing dark panel. Use a small quiet SF Mono / native macOS monospaced font, regular weight, light muted gray, tabular digits. This is an elapsed-duration display, not a date or clock-of-day. At the intended native panel size of about 192 x 44 logical points the timer would be approximately 9-point type. In this magnified image make the timer visually small and secondary, about 35–40 pixels high, much smaller than the microphone glyph or Stop button. Leave a small clear gap between the timer and the level segments. Use the empty space above the meter; keep the meter itself unobscured and at its existing position.

Preserve the exact selected composition and all other pixels as closely as possible: the tiny landscape dark rounded panel, dimensions and height, cyan microphone on the left, central cyan segmented level bar, red circular Finish button with white stop square, gray downward hide chevron, subtle materials and shadow, plain light background, and front-on framing. Do not enlarge the panel. Do not change button shapes, icons, colors, spacing, hierarchy, or add borders, extra controls, source names, captions, titles or any other text. The ONLY visible text is "04:12". Remain permanently dark. Keep one single panel in the output. Match the source image canvas dimensions and aspect ratio (approximately 1774 x 886 pixels).
