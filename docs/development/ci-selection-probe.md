# CI selection probe

This temporary document verifies that documentation-only changes select portable
formatting and an aggregate result without starting server or native checks.
The verification PR will be closed without merging this document.
